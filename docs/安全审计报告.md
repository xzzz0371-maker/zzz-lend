# ZZZ Lend — 安全审计报告（首席安全审计员，只审不改）

> **报告版本：v1.1（2026-08-31）** ｜ 审计日期：2026-08-31 ｜ 范围：`contracts/src/` 全部 8 个合约逐行审查
> 原则：**本阶段只审查，不修改代码**。发现的问题与修复建议待审核后统一修复。
> 基线：143 个单元测试全过、Sepolia 部署 + E2E 全过。本报告以攻击者视角检查 18 类攻击向量。
> 版本更新约定：每次复检/修复后递增版本号（v1.0 → v1.1 …），并更新日期。

---

## 修复记录（v1.1 · 2026-08-31）

v1.0 报告的代码问题已按 P0/P1/P2 修复，**158 个测试全绿**。修复内容（代码）见 `docs/修复记录_v1.1.md`，各问题当前状态：

| 编号 | 级别 | 问题 | 状态 |
|---|---|---|---|
| F1 | P0/中 | repay/liquidate 取整下溢 | ✅ 已修复（normReduction 按 borrowNorm 封顶） |
| F3 | P1/中 | SwitchableOracle.setPrice 无校验 | ✅ 已修复（price>0 + 范围 [1e2,1e14]） |
| F4 | P1/中 | 模块 setter 缺零地址校验 | ✅ 已修复（零地址 + setReserveManager 合约校验） |
| F2 | P2/低 | 坏账超额损失破坏守恒 | ✅ 已修复（级联吸收：存款人→账面储备→Treasury） |
| F5 | P2/中 | 偏差标记依赖 updatePrice + fallback | ✅ 已修复（isPriceAnomalous 实时偏差 + lastValidPrice 基准；fallback 取舍文档化） |
| F9 | P2/低 | oracle 暂停阻断清算 | ✅ 已文档化设计取舍 + 测试固化当前行为 |
| F6 / F11 | 高 | 管理员中心化 / 存款人损失无上限 | ⏳ 治理/产品决策，不在代码修复范围 |
| F7 / F8 / F10 | 低 | MockUSDC faucet / 清算激励 / 溢出只转账面 | ⏳ 设计选择或测试网专用，不改代码 |

---

## 0. 执行摘要（风险定级）— v1.1 · 2026-08-31

| 级别 | 数量 | 编号 | 一句话 | 备注（日期） |
|---|---|---|---|---|
| **高危（系统性/设计）** | 2 | F6, F11 | 管理员中心化（部署者握全部角色，可换模块/设任意价）；存款人损失无上限 + 储备缓冲薄（3%） | 2026-08-31 |
| **中危** | 4 | F1, F3, F4, F5 | repay 全量还款取整下溢卡死小额仓位；setPrice 无校验（可设 0/无上界）；模块 setter 缺零地址校验；fallback 冻结价格 + 偏差仅 updatePrice 更新 | 2026-08-31 |
| **低危** | 5 | F2, F7, F8, F9, F10 | 无存款人时坏账不守恒（边缘）；MockUSDC faucet 无限铸币；深水下清算激励缺失；oracle 暂停阻断清算；溢出只转账面储备 | 2026-08-31 |
| 无漏洞 | 11 类 | — | 重入 / 闪电贷 / 价格数学 / 清算数学 / 坏账主逻辑 / 溢出防护 / 精度 / ERC20 / DoS / MEV / 利息 / LTV/HF / 暂停 / 储备会计 / 守恒（主路径） | 2026-08-31 |

> **注**：v1.1 中 F1/F3/F4/F2/F5/F9 已修复（见修复记录）；上表为 v1.0 发现的原始清单，供追溯。

---

## 1. 高危发现

### F6（High，设计/治理）：管理员中心化 = 单一故障点 + 潜在 rug 面
- **位置**：所有合约构造器把 DEFAULT_ADMIN / PARAM_ADMIN / PAUSER 全部授予部署者；`InterestRateModel`/`RiskManager`/`ReserveManager` 为 Ownable（owner=部署者）。
- **风险面**：
  - `LendingPool.setPriceOracle / setInterestRateModel / setRiskManager / setLiquidationManager / setReserveManager`：PARAM_ADMIN **可换任意模块地址**，换成恶意合约即可：`setReserveManager(恶意)` 令坏账资金转走；`setLiquidationManager(恶意)` 令清算没收量异常；`setPriceOracle(恶意)` 注入任意价格 → **实质 rug 能力**。
  - `SwitchableOracle`（PAUSER 可 `enableSettable`）+ `setPrice`（PARAM_ADMIN 可设**任意价**）：若测试后忘记 `disableSettable()`，池子永久运行在管理员设定价上（E2E 用的就是这个通道）。
  - `RiskManager.setTier` 可把 maxLTV 提到 90%、`setCloseFactor` 提到 100%：恶化风险参数。
  - `ChainlinkOracle.setFeed` 可换任意聚合器地址（无需真实 feed）。
- **结论**：非代码漏洞，但**生产上线前必须**：多签持有角色、部署者 `renounceRole`/移交、设置 `TESTNET_ADMIN` 并撤销部署者。测试网可接受（文档 §5/§6.6 已列）。
- **修复建议**：部署脚本移交三角色给多签并 `renounceRole(DEFAULT_ADMIN_ROLE, deployer)`；模块 setter 加 zero-address + 模块类型校验（或升级代理方案）。

### F11（High，设计/系统性风险）：存款人损失无上限 + 坏账缓冲薄弱
- **位置**：`handleBadDebt` 的 supplyIndex 削减公式 `newIndex = old × (supply - loss)/supply`，`loss ≥ supply` 时 index 直接归零（存款人 100% 归零）。
- **场景**：ETH 极端跌幅（如 -80%）→ 大量 tier5 仓位同时进入"抵押归零 + 残余债务"，储备（目标仅 `totalBorrows×3%`）瞬间耗尽，剩余损失全部传导给存款人，可能**一次事件抹掉大部分/全部存款**。
- **结论**：会计正确但**经济设计激进**；无单点代码漏洞。
- **修复建议**：提高储备目标 / 引入坏账分批摊销上限 / 对高 LTV 档位更早清算阈值；这是产品决策，需你审核。

---

## 2. 中危发现

### F1（Medium，已实证并修复）：`repay()` 全量还款取整下溢 → 小额仓位无法清债
- **位置**：`LendingPool.sol` `repay()` 与 `liquidate()`（修复前）。
- **根因**：`_getDebt6` 向上取整 `ceil(norm×index/WAD)`，而 `repay` 的 `normReduction = repayAmount×WAD/index`（向上取整）。当借款额很小（borrowNorm 与 1 个 USDC 微单位同量级）且计息后 `index > WAD` 时，`debt6` 进位到 `borrowNorm+1`，`normReduction` 也进位到 `borrowNorm+1` → `pos.borrowNorm -= normReduction` **下溢 revert**。
- **修复**：`repay`/`liquidate` 的 `normReduction` 增加 `if (normReduction > pos.borrowNorm) normReduction = pos.borrowNorm;`（与 handleBadDebt 一致）。回归测试见 `SecurityFixes.t.sol::test_F1_*`。
- **影响**：仅影响尘埃仓位（< ~0.02 USDC）；修复后不再 revert。

### F3（Medium，已修复）：`SwitchableOracle.setPrice` 无 `price>0` 与上界校验
- **位置**：`SwitchableOracle.setPrice`（修复前仅校验 `asset != 0`）。
- **修复**：`require(price > 0)` + `require(price >= 1e2 && price <= 1e14)`。回归测试 `test_F3_*`。

### F4（Medium，已修复）：模块 setter 缺零地址校验
- **位置**：`LendingPool` 5 个模块 setter（修复前无校验）。
- **修复**：全部加 `require(address(x) != address(0), "zero address")`；`setReserveManager` 额外校验 `code.length > 0`。回归测试 `test_F4_*`。

### F5（Medium，已修复）：fallback 冻结缓存价 + 偏差标记仅 `updatePrice` 更新（监控缺口）
- **位置**：`ChainlinkOracle` `useFallback` / `_getFallbackPrice` / `priceAnomalous`。
- **修复**：新增 `lastValidPrice` 稳定基准，`isPriceAnomalous` 改为实时内联计算偏差（不再依赖 updatePrice）；fallback 取舍文档化。回归测试 `test_F5_*`。

---

## 3. 低危发现

- **F2（Low，已修复）**：`handleBadDebt` 当 `getTotalSupply()==0` 时跳过 supplyIndex 削减，`lossToDepositors` 无处吸收 → 资金守恒被破坏。**修复**：级联吸收（存款人 → 账面 `totalReserve` → `treasuryAccrued`），由不变式保证损失必可完全吸收，守恒恒成立。回归测试 `test_F2_*`。
- **F7（Low，部署性）**：`MockUSDC.faucet` 无权限任意铸币。**仅限测试网**；若误上主网 = 无限增发灾难。主网部署用真实 USDC。
- **F8（Low，激励）**：深度水下仓位（抵押价值 ≪ 50% 债务）清算人会亏本 → 无人清算 → 坏账传导给存款人。属经济激励缺口。
- **F9（Low，已文档化）**：oracle `pause` 后 `getAssetPrice` revert → `liquidate` 也被阻断。取舍已文档化（宁可不清算，也不用错误价清算），回归测试 `test_F9_*` 固化当前行为。
- **F10（Low，会计）**：储备溢出只从**账面 `totalReserve`** 划转；物理储备超标部分不转 Treasury（安全设计）。

---

## 4. 18 类攻击向量逐项结论 — v1.1 · 2026-08-31

| # | 向量 | 结论 | 说明 |
|---|---|---|---|
| 1 | 重入 | ✅ 无漏洞 | 全部状态函数 `nonReentrant`；`_safeSendEth` 用 `call` 但在 Guard 内且 CEI；USDC transferFrom 在状态更新后；`accrue()` 仅内部+view 调用。 |
| 2 | 访问控制 | ⚠️ 已缓解（F4 修复，F6 待治理） | 三角色隔离正确；无函数漏加权限；管理员无绕过 LTV/HF/清算、无直改用户仓位/supplyIndex 的入口；v1.1 已封堵零地址模块脚枪；部署者角色集中待多签。 |
| 3 | 预言机操纵 | ⚠️ 已缓解（F3/F5 修复） | Chainlink stale/deviation/pause 逻辑正确；v1.1 `isPriceAnomalous` 实时偏差 + `lastValidPrice` 稳定基准；`setPrice` 已加范围校验；fallback 取舍文档化。 |
| 4 | 闪电贷 | ✅ 无漏洞 | 无 flashLoan 入口；利息按时间（同区块 dt=0 零利息）；借还同块净零；利用率瞬时操纵在还款后回落；价格是 Chainlink，DEX 砸盘无效。 |
| 5 | 价格操纵 | ✅ 无漏洞 | 唯一价格源 = Chainlink/可设价（admin）；ETH/USDC 换算小数位正确（8/6/18）；价格 0/极端值受 `>0` 校验、溢出距离远（~1e30 ≪ 2^256）。 |
| 6 | 清算攻击 | ✅ 无漏洞（附 F8） | 禁自清算；closeFactor 50% 正确；seize=cover×1.05 封顶于抵押总值；minSeize 只限下限；重复清算正常；异常价不造成超额获利；部分清算后利息续算正确。 |
| 7 | 坏账/资不抵债 | ✅ 主逻辑无漏洞（F2 已修复） | 触发条件正确；supplyIndex 公式精确守恒；v1.1 级联吸收保证超额损失也被完全吸收，守恒恒成立；无法伪造坏账；**风险在设计（F11）**。 |
| 8 | 溢出/下溢 | ⚠️ **F1 已修复** | 全合约无 `unchecked`；大数运算安全；v1.1 已封堵 repay/liquidate 的 normReduction 下溢（按 borrowNorm 封顶）。 |
| 9 | 精度损失 | ✅ 无漏洞（F1 已修复） | 份额/取款向下取整、债务向上取整，方向利于协议；按秒计息精度足够；微小金额主路径无损。 |
| 10 | ERC20 | ✅ 无漏洞 | transfer/transferFrom 返回值全部检查；approve 竞态不适用；6 位小数全路径正确；**MockUSDC.faucet 仅限测试网（F7）**。 |
| 11 | DoS | ⚠️ 已缓解（F3/F4 修复，F9 文档化） | 无攻击者可永久卡死路径；v1.1 封堵零地址模块与 setPrice=0 脚枪；oracle pause 阻断清算为文档化取舍。 |
| 12 | 抢跑/MEV | ✅ 无漏洞（评估影响） | 清算被抢跑 = 标准竞态，仅影响 bonus 归属；大额存/借抢跑仅微小影响利率；可设价模式下 admin 设价无抢先套利空间。 |
| 13 | 利息计算 | ✅ 无漏洞 | `_accrue` 每档指数计息正确；supplyIndex 增长正确；部分还款后续息基准正确；同区块零利息；长周期/换预设连续；利用率 0/100% 无除零。 |
| 14 | LTV/HF | ✅ 无漏洞 | maxBorrowable/validateBorrow/HF 计算正确；tier locked 与换档正确；抵押/债务增减后 LTV/HF 正确更新。 |
| 15 | 暂停 | ✅ 无漏洞（F9 文档化） | 暂停禁 supply/supplyCollateral/borrow，放行 withdraw/repay/liquidate/handleBadDebt；oracle 暂停连带阻断 liquidate 为已文档化取舍。 |
| 16 | 管理员/参数 | ⚠️ 已缓解（F4/F6） | 参数有上下限；v1.1 封堵零地址模块；**模块替换与角色集中仍是最大治理风险（F6 待多签）**。 |
| 17 | 储备/Treasury | ✅ 主逻辑无漏洞（附 F10） | 92/5/3 固定分配正确；目标=3%×totalBorrows；溢出转 Treasury 正确；totalBorrows=0 不溢出；坏账消耗储备后溢出停止；collectTreasury 清零+零地址 revert；储备无任意提款路径。 |
| 18 | 资金守恒 | ✅ 恒成立（F1/F2 已修复） | 存/取/借/还/清算/利息/溢出后均守恒；v1.1 封堵 repay 下溢（revert 类）与坏账超额损失（级联吸收），守恒在全部路径成立。 |

---

## 5. 审计中未覆盖但建议补充的检查项（自定义补充）

1. **同区块多次操作组合**：`supply→borrow→repay→withdraw` 在单一交易内的净效果（E2E 跨区块已验，未做同区块原子组合验证）。
2. **`withdrawCollateral` 在 oracle 异常/暂停时的行为**：有债时读价，oracle 故障则抵押品被锁（建议纳入 fallback 放行或紧急提款路径）。
3. **`supplyIndex=0` 后遗症**：一旦归零，`supply()` 除零 revert，池子对新增存款永久关闭；建议在上限兜底（如损失超过 95% 时触发系统级处理，而非静默归零）。
4. **换模块后的兼容性**：`setInterestRateModel` 换到不同接口实现的模型时，`getBorrowRatePerSecond` 语义差异可能导致异常利率。
5. **`test-out/` 覆盖数据**：覆盖率报告只针对三个核心合约 >90%，`InterestRateModel/RiskManager` 的边界未全部纳入压力矩阵。
6. **`getBorrowAPR` 用 `getUtilization()` 的瞬时值**：展示用，与 `_accrue` 内部同一时刻的 utilization 一致，无偏差。
7. **E2E 中 USDC feed stale 的兜底**：借/还/清算依赖 USDC 价，feed 长 stale 时功能全停；建议加入"价格停更超时后使用 fallback 缓存"或紧急暂停策略。

---

## 6. 修复优先级建议 — v1.1 · 2026-08-31（v1.0 修复项已完成）

| 优先级 | 项 | 状态 |
|---|---|---|
| P0 | F1：`repay`/`liquidate` 的 `normReduction` 封顶 | ✅ 已修复 |
| P1 | F4：模块 setter 零地址校验 | ✅ 已修复 |
| P1 | F3：`setPrice` 加 `price>0` + 上下界 | ✅ 已修复 |
| P1 | F6：部署脚本移交角色 + 撤销部署者（多签） | ⏳ 待部署时执行（治理） |
| P2 | F5：`isPriceAnomalous` 实时偏差 + `lastValidPrice` | ✅ 已修复 |
| P2 | F2：坏账级联吸收保证守恒 | ✅ 已修复 |
| P3 | F8/F9/F11：经济参数与产品决策 | ⏳ 需讨论（F9 已文档化） |

---

*审计结论：v1.0 发现的可直接导致资金/功能异常的中低危代码问题（F1/F3/F4/F2/F5/F9）已全部修复并通过 158 个测试；剩余为治理（F6）、产品设计（F11）与设计取舍（F7/F8/F10）。报告与修复记录见 `docs/修复记录_v1.1.md`。*
