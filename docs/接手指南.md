# ZZZ Lend — 接手指南（Developer Handoff）

> **用途**：让新开发者能独立接手本项目。完整设计见 `docs/ZZZ_Lend_完整文档.md`，本文件聚焦"怎么上手 + 关键实现 + 部署运维"。
> 更新时间：2026-08-31 ｜ 状态：阶段 7 已部署 Sepolia（地址见完整文档 §6.7），待链上 E2E/清算演示

---

## 0. 10 分钟快速上手 — v1.0 · 2026-08-31

```bash
# 环境要求：Windows / Git Bash（Foundry 自带）或 WSL；需安装 Git、Foundry、Node.js
# 1) 进入合约工程
cd contracts

# 2) 安装依赖（若 lib/ 为空）
forge install foundry-rs/forge-std OpenZeppelin/openzeppelin-contracts
npm install

# 3) 跑全部测试
forge test            # 期望：23 suites / 173 passed / 0 failed

# 4) 格式化 + lint
forge fmt --check
npm run lint          # 0 errors

# 5) 本地起链并模拟部署 + 端到端
anvil                                  # 另开终端
MOCK_FEEDS=1 forge script script/Deploy.s.sol:Deploy --rpc-url http://127.0.0.1:8545 --broadcast
# 再开一个终端执行 E2E（cast 版）
export RPC_URL=http://127.0.0.1:8545
export DEPLOYER_KEY=0xac09...2ff80   # anvil 默认账号0
export USER_A_KEY=0x59c6...78690d    # anvil 账号1
export USER_B_KEY=0x5de4...b365a     # anvil 账号2
bash script/e2e_sepolia.sh           # 全 8 步 status=0x1，HF 1.114→0.867→清算→0.914

# 6) 前端 Dashboard（连接 Sepolia 已部署合约，端口 3000）
cd ../frontend
npm install
npm run dev            # http://localhost:3000（MetaMask 连接）
```
> 前端函数名/ABI 说明与已知问题见 `frontend/README.md`；实现与验证见 `docs/前端_验证报告_v1.0.md`。

---

## 1. 项目概览

- **是什么**：以"风险分层 + 收益/风险/资本效率透明"为核心的 DeFi 借贷协议。存款人存 USDC 赚动态预估 Supply APY（随市场利率和资金利用率实时变化，不构成固定收益承诺）；借款人抵押 ETH、选五档 LTV（50/60/70/75/80%）借 USDC。
- **一句话**：用户自己选风险，系统实时给风险定价。
- **技术栈**：Solidity 0.8.24 + Foundry 1.8.1 + OpenZeppelin v5.7 + Chainlink + Solidity 原生测试；**前端已实现**（Next.js 14 + wagmi，连接 Sepolia，见 `frontend/README.md`）。
- **当前阶段**：合约 + 测试全部完成（126 用例）；Sepolia 部署脚本就绪，**链上广播待凭据**（RPC / 测试网钱包 / Etherscan Key）。

---

## 2. 环境与工具链 — v1.0 · 2026-08-31

| 工具 | 版本 | 安装/位置 | 备注 |
|---|---|---|---|
| Git | ≥2.40 | 系统 PATH | Foundry 依赖 submodule |
| Foundry (forge/anvil/cast) | 1.8.1 | `~/.foundry/bin`（已加 PATH） | 用 `foundryup` 或官方 release 二进制 |
| Node.js / npm | ≥20 | 系统 PATH | 仅 lint 用 |
| Python | 3.x | 系统 PATH | 供 `script/e2e_sepolia.sh` 解析 JSON |

> 工具链已在本机装好；换机后按上表重装。Windows PowerShell 会话需刷新 PATH 才能用新装的命令。

---

## 3. 目录结构

```
test_opencode/
├── README.md                        # 总入口（指向完整文档）
├── docs/
│   ├── ZZZ_Lend_完整文档.md          # ★ 单一总文档（产品/合约/测试/部署/风险）
│   └── 接手指南.md                   # 本文档
├── frontend/                        # 前端 Dashboard（Next.js 已实现：Home/Dashboard/Stress Test/History/About，见 README）
├── services/                        # 预留（风险引擎服务规划）
└── contracts/                       # ★ Foundry 合约工程（主要工作区）
    ├── foundry.toml                 # solc 0.8.24 / cancun / optimizer / via_ir
    ├── remappings.txt               # @openzeppelin、forge-std 映射
    ├── .env.example                 # 环境变量模板（RPC/私钥/feed/管理员）
    ├── .solhint.json / package.json # lint 与 npm 脚本
    ├── src/
    │   ├── LendingPool.sol          # ★ 主合约（存/借/清算/坏账/储备/利息）
    │   ├── InterestRateModel.sol    # 利率曲线 + 三套市场预设
    │   ├── RiskManager.sol          # 五档 LTV/清算阈值/closeFactor/maxBorrowTier
    │   ├── LiquidationManager.sol   # 清算没收量计算
    │   ├── ReserveManager.sol       # 风险储备
    │   ├── interfaces/IPriceOracle.sol
    │   ├── oracle/ChainlinkOracle.sol   # Chainlink 适配器（含安全机制）
    │   ├── oracle/SwitchableOracle.sol  # 可切换预言机（清算演示用）
    │   ├── risk/RiskEngine.sol      # 风险等级 + 波动率 + calculateRiskLevelAuto
    │   └── mocks/                   # MockUSDC / MockPriceOracle / MockAggregatorV3
    ├── test/                        # 20 个测试文件 / 126 用例
    ├── script/
    │   ├── Deploy.s.sol             # Sepolia 部署脚本
    │   ├── Verify.s.sol             # 打印 verify 命令
    │   ├── TestnetE2E.s.sol         # forge 版 E2E（参考）
    │   └── e2e_sepolia.sh           # ★ cast 版 E2E（已本地验证，推荐）
    └── deployments/                 # 部署输出 sepolia.json（已 gitignore）
```

---

## 4. 合约架构与关键逻辑（读代码前必看）

### 4.1 会计模型（最重要）
- **USDC 存款**用份额记账：`shares = amount × 1e18 / supplyIndex`；取款 `amount = shares × supplyIndex / 1e18`。无 aToken。
- **利息累计**（`_accrue`）：按档位独立 `borrowIndexByTier[t]` 指数计息，`supplyIndex` 随总利息增长；**固定费率**：存款人 94% / 储备 `reserveFactor=4%` / Treasury `treasuryFactor=2%`；储备目标 = `totalBorrows × 3%`，超出自动溢出转 Treasury（`ReserveOverflowTransferred`，只从账面 `totalReserve` 划转，物理储备仅由坏账消耗；`totalBorrows=0` 不触发溢出）；`collectTreasury()` 提取 Treasury 并清零 `treasuryAccrued`。
- **Early Deposit Boost（§3.5）**：Tier1+2 借款利息分摊后若低于 2% APY，从 `boostPool` 补足到 2%（前 6 个月、每钱包 1 万上限、只补利息不保本）；`claimBoost` 领取、`fundBoost` 注入、`setBoostParams` 配置。
- **债务计价（改动后）**：`debtWad = USDC数量 × USDC/USD价格`；抵押价值 `= ETH × ETH/USD价格`。LTV/HF/可借额全部基于美元价值。
- **核心不变式（每条资金路径后测试断言）**：`cash + totalBorrows == getTotalSupply() + totalReserve + treasuryAccrued + boostPool`（USDC 单位，与价格无关）。

### 4.2 主要入口函数 — v1.0 · 2026-08-31
| 函数 | 权限 | 说明 |
|---|---|---|
| `supply(amount)` / `withdraw(shares)` | 无 | 存/取 USDC（share 单位取款）；`supply` 需 ≥ `MIN_SUPPLY=10 USDC` |
| `supplyCollateral()` (payable) / `withdrawCollateral(amount)` | 无 | 抵押/取回 ETH；`supplyCollateral` 需 ≥ `MIN_COLLATERAL=0.01 ETH` |
| `borrow(amount, tier)` / `repay(amount)` | 无 | 借（选档，需 ≥ `MIN_BORROW=100 USDC`）/还（type.max=全额） |
| `liquidate(target, debtToCover, minSeizeAmount)` | 无 | 清算（minSeize=0 不限制） |
| `handleBadDebt(target)` | 无 | 坏账即时传导（储备覆盖 + supplyIndex 降值） |
| `skimReserve()` / `accrue()` | 无 | 储备归集 / 手动计息 |
| `collectTreasury()` | 无 | Treasury 归集：`treasuryAccrued` 全部转给 `treasuryAddress` 并**清零**（非零地址方可调用） |

### 4.3 五档 LTV 与清算 — v1.0 · 2026-08-31
| 档 | 最大 LTV | 清算阈值 | 利率溢价 |
|---|---|---|---|
| 1 | 50% | 60% | 0 |
| 2 | 60% | 70% | +0.5% |
| 3 | 70% | 78% | +1.5% |
| 4 | 75% | 85% | +3% |
| 5 | 80% | 90% | +6% |

HF<1 可清算，closeFactor 50%，清算人按 105% 估值没收 ETH；一个用户同时只占一个档位（换档需先还清）。

### 4.4 利率模型与市场预设
`InterestRateModel`：`rate = base + slope1×util（≤kink）+ slope2×(util−kink)（>kink）+ tierPremium`。三套预设可切换（NORMAL base=0.5%、slope1=4%、kink=80%、slope2=50%、档位溢价 0/1/2/3/4.5% / HIGH_VOL base=2% / EXTREME base=5%；EXTREME 时 RiskManager.maxBorrowTier=3 暂停高 LTV 档）。

### 4.5 Oracle 体系（三层）
1. **ChainlinkOracle**：连真实聚合器；stale 超 1h revert；偏差超 **maxDeviation（默认 30%，范围 10%~200%）** 不 revert，`updatePrice` 触发 `PriceAnomalyDetected` 并标记 `priceAnomalous[asset]`；fallback（缓存价，限 1 天）；PAUSER 可 pause。
2. **SwitchableOracle**：主源=ChainlinkOracle；PAUSER 可 `enableSettable()`/`disableSettable()` 切到"管理员可设价"模式（用于测试网清算演示）；可设价模式 `isPriceAnomalous` 返回 false。
3. **LendingPool 消费侧**：`_oracleAnomalous()` 检查 ETH/USDC 标记；**异常时 `borrow`/`supplyCollateral` revert，`repay`/`withdraw`/`liquidate` 正常**。

### 4.6 坏账（即时传导，不挂账）
抵押归零仍有债 → `handleBadDebt`（任何人可调）先由风险储备（第一损失缓冲）覆盖可覆盖部分，未覆盖部分即时降低 `supplyIndex`（所有存款人按份额承担，类似基金净值下跌），仓位一次性清零。事件 `BadDebtRealized(user, badDebtAmount, coveredByReserve, lossToDepositors, oldSupplyIndex, newSupplyIndex)`。**无 `settleBadDebt`、无 `totalBadDebt`**；存款本金可能因坏账而受损。坏账消耗储备后储备低于目标，溢出机制停止，重新开始积累。

### 4.7 RiskEngine
只输出 LOW/MEDIUM/HIGH/EXTREME 建议，**不触碰资金**。`calculateRiskLevelAuto(ltv, lt)` 从池直读 Utilization/流动性 + 引擎波动率采样；手动版 `getRiskLevel(vol,util,ltv,lt,liq)` 保留供测试。

---

## 5. 权限模型（部署后角色矩阵）— v1.0 · 2026-08-31

| 合约 | PARAM_ADMIN | PAUSER | DEFAULT_ADMIN / Owner |
|---|---|---|---|
| LendingPool / ChainlinkOracle / SwitchableOracle | 部署者 + TESTNET_ADMIN | 部署者 + TESTNET_ADMIN | **仅部署者** |
| RiskEngine | 部署者 + TESTNET_ADMIN | — | **仅部署者** |
| InterestRateModel / RiskManager / ReserveManager | — | — | Owner = TESTNET_ADMIN |

- **部署者始终是最终管理者**（DEFAULT_ADMIN 未移交 + 未撤销构造器角色）。测试网可接受；**生产必须移交多签并撤销部署者角色**。
- 管理员能：改参数/换模块/暂停/切预设；**不能**：动用户资金、绕过 LTV/HF/清算、注入任意价格、直接降低 supplyIndex（只能经 `handleBadDebt` 触发）。

---

## 6. 测试体系 — v1.0 · 2026-08-31（每次跑完测试更新用例数）

```bash
forge test                    # 173 passed
forge test --match-contract X  # 单文件
forge test -vvv / -vvvv        # 详细/极详细
forge coverage --report lcov --ir-minimum   # 覆盖率
npm run lint                  # 0 errors
```

**测试文件速查**（`test/`）：
- `LendingPool.t.sol`(14) 存借清算/利息/HF/利用率/流动性限制
- `Liquidation.t.sol`(7) 清算规则/坏账
- `StressTest.t.sol`(1×30) 6跌幅×5档压力矩阵（生成 `test-out/stress_matrix.md`）
- `Security.t.sol`(3) 重入
- `Precision.t.sol`(6) 极小金额精度
- `Repay.t.sol`(5) 部分还款
- `RatePresets.t.sol`(6) 三套利率预设
- `Oracle.t.sol`(11) 预言机安全 + 异常价禁借
- `SwitchableOracle.t.sol`(7) 可切换预言机
- `RiskEngine.t.sol`(12) 风险等级 + Auto
- `BadDebt.t.sol`(9) 坏账即时传导（储备覆盖/supplyIndex 降值/多存款人/取款与利息/事件）
- `SecurityFixes.t.sol`(20) 审计 v1.1 修复回归（F1 取整下溢/F3 setPrice 校验/F4 零地址/F2 超额损失守恒/F5 实时偏差/F9 oracle 暂停清算）+ v1.2 最小金额限制
- `Boost.t.sol`(10) Early Deposit Boost（Tier1+2 利息保底 2%：利用率 0/10/50%、上限 1 万、到期、Tier345 不参与、权限、守恒）
- `FeeMechanism.t.sol`(11) 固定费率 92/5/3 + 储备溢出转 Treasury（含坏账耗尽储备后溢出停止）
- `MinSeize.t.sol`(3) 清算最小没收量
- `EthTransfer.t.sol`(3) call 转账/重入
- `UsdcPeg.t.sol`(5) USDC 溢价/脱锚
- `Events.t.sol`(6) 事件参数
- `LiquidationTimeGap.t.sol`(1×9) 清算时间差
- `Permissions.t.sol`(8) / `ReserveManager.t.sol`(7) / `RiskManager.t.sol`(8) / `InterestRateModel.t.sol`(9)

**覆盖率**（`forge coverage`）：LendingPool 91.8%、LiquidationManager 100%、RiskManager 100%（三个核心均 >90%）。

---

## 7. 安全机制与已知风险

**已实现**：ReentrancyGuard + `call{value}` 转账；CEI；份额/指数记账 + 取整对协议有利；USDC 脱锚计价；Oracle stale/偏差/fallback/pause + 异常禁新增敞口；暂停开关；事件全量；**坏账即时传导给存款人**（超出储备缓冲部分即时降低 `supplyIndex`，存款本金可能受损）；**储备溢出自动转 Treasury**（超过 `totalBorrows × 3%` 自动转出，储备不无限膨胀）；前端须按完整文档 §1.6 展示风险披露。

**已知风险 / 待办**：
1. 管理员集中 → 生产移交多签、撤销部署者。
2. `ChainlinkOracle.setFeed` 可换价格源地址 → 生产多签 + 审计。
3. 偏差容忍副作用：极端跳价以异常价执行（为保清算）→ 事件监控 + PAUSER 暂停兜底。
4. 未经外部安全审计（阶段 8）。
5. lint 393 warnings（require→custom errors 风格，非阻塞）。
6. Git 仓库已 init 但**未提交**、未配 user.name/email。

---

## 8. Sepolia 部署（详细）

### 8.1 前置（.env，已 gitignore）
```
SEPOLIA_RPC_URL=...                                  # Alchemy/Infura
PRIVATE_KEY=0x...                                    # 测试网专用，cast wallet new 生成
ETHERSCAN_API_KEY=...
SEPOLIA_ETH_USD_FEED=0x694AA1769357215DE4FAC081bf1f309aDC325306
SEPOLIA_USDC_USD_FEED=0xA2F78ab2355fe2f984D808B5CeE7FD0A93D5270E
TESTNET_ADMIN=0x...                                  # 可选，缺省=部署者
E2E_USER_A_KEY / E2E_USER_B_KEY                       # E2E 用户私钥（需测试网 ETH）
```

### 8.2 部署
```bash
forge script script/Deploy.s.sol:Deploy --rpc-url $SEPOLIA_RPC_URL --broadcast --verify -vvvv
```
顺序：MockUSDC → ChainlinkOracle(+MockPriceOracle) → SwitchableOracle → InterestRateModel → RiskManager → LiquidationManager → ReserveManager → **LendingPool** → **RiskEngine**（构造需池地址，故在池后）→ 接线/授权。结果写 `deployments/sepolia.json`。

### 8.3 验证
```bash
forge script script/Verify.s.sol:Verify --rpc-url $SEPOLIA_RPC_URL   # 打印全部 verify 命令
# 逐合约：forge verify-contract <addr> <Name> --chain 11155111 --compiler-version v0.8.24 --etherscan-api-key $ETHERSCAN_API_KEY --watch
```

### 8.4 端到端 + 清算演示
```bash
bash script/e2e_sepolia.sh
```
8 步：A 存 1000 USDC → deployer 补流动性 → B 存 10 ETH → B 借 21000@tier3(HF=1.114) → 状态检查 → B 部分还款 → A 提取 → **清算演示**：PAUSER `SwitchableOracle.enableSettable()` → PARAM_ADMIN `setPrice(ETH, 2100e8)`（-30%，另设 USDC=1e8）→ liquidate（HF 0.867→0.914）→ PAUSER `disableSettable()` 切回 → 守恒检查。

> **注意**：可设价模式下需为 ETH 与 USDC **都**设价；演示后必须 `disableSettable()` 切回真实 Chainlink。

---

## 9. 设计决策记录（Q&A）

1. **ETH 转账**：原生 ETH，`call{value}`（`_safeSendEth`），失败 revert；不受 2300 gas；重入被 Guard 拦截。
2. **ReentrancyGuard**：全部状态函数。
3. **minAmount**：V1 无 AMM 无滑点；清算有 `minSeizeAmount`（0=不限）。
4. **70%→78% 清算阈值**：有意设计，可配。
5. **USDC 计价**：债务=USDC×USDC/USD 价格；USDC=1 时与旧行为一致；溢价抬 LTV、脱锚降 LTV（UsdcPeg 5 场景覆盖）。
6. **Oracle 偏差**：不 revert、事件化、异常禁新增敞口；PAUSER 可暂停。
7. **RiskEngine**：纯建议；Auto 从池直读；liquidity 有 0~1 校验。
8. **无绕过 oracle 校验的读价路径**。

---

## 10. 待办与路线图

1. **Sepolia 真部署**（待凭据），回填地址到完整文档 §6.7。
2. ✅ 前端（`frontend/`）：Dashboard / Choose Your Risk / Stress Test / History / About 已实现（全英文，见 `frontend/README.md`）。
3. 数据服务（`services/`）+ Subgraph。
4. 生产权限收口（多签/撤销部署者）、外部安全审计。
5. Git 首次提交（配 user.name/email 后 commit）。

---

## 11. 常见问题排查 — v1.0 · 2026-08-31

| 现象 | 原因 | 处理 |
|---|---|---|
| `InsufficientFunds` | 钱包无测试网 ETH | faucet 领 ETH |
| `stale price` | 喂价超 1h 未更新 | 等更新或换 feed |
| `price anomalous` | 偏差>30% 被标记 | 等价格回归；repay/withdraw/liquidate 不受限 |
| `insufficient liquidity` | 池现金不足 | 增加存款 |
| `ltv too high` | 借款超档位上限 | 用 `maxBorrowable` |
| `price not set` | SwitchableOracle 可设价模式漏设价 | 为 ETH 和 USDC 都 `setPrice` |
| `oracle paused` | 被 PAUSER 暂停 | `unpause()` |
| forge broadcast 清算笔偶发失败 | forge 批处理/非ce 怪癖 | 用 cast 版 `e2e_sepolia.sh` |
| `stack too deep`（coverage） | 覆盖率禁 via_ir | 加 `--ir-minimum` |

---

## 12. 接手 Checklist（新人完成即算接手成功）

- [ ] 跑通 §0 快速上手（forge test 126 绿、Anvil 部署 + e2e 全过）
- [ ] 读懂 §4（会计模型 / 不变式 / 五档 LTV / Oracle 三层 / 坏账）
- [ ] 读懂 §5 权限模型，能说出部署者与 TESTNET_ADMIN 的区别
- [ ] 能跑覆盖率并看懂三个核心合约行覆盖
- [ ] 阅读 `InterestRateModel` / `RiskManager` / `LendingPool._accrue` 三个关键文件源码
- [ ] 了解 Sepolia 部署与清算演示步骤（§8）
- [ ] 记录一次 `git commit`（先 `git config user.name/email`）

---

*本指南由交接文档模板生成，随项目推进更新。*
